# 🏭 FactoryAI 智工助手 - 技术架构设计

## 版本：v1.0

---

## 一、系统架构

### 1.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        客户端层                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│  │   iOS App   │  │ Android App │  │   Web App   │            │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘            │
└─────────┼────────────────┼────────────────┼─────────────────────┘
          │                │                │
          └────────────────┼────────────────┘
                           │
                    ┌──────▼──────┐
                    │   API 网关   │
                    │   (Nginx)   │
                    └──────┬──────┘
                           │
          ┌────────────────┼────────────────┐
          │                │                │
┌─────────▼─────────┐ ┌────▼────┐ ┌────────▼────────┐
│   用户/鉴权服务   │ │ 任务服务 │ │   设备服务     │
│    (Node.js)     │ │(Node.js) │ │   (Node.js)    │
└─────────┬─────────┘ └────┬────┘ └────────┬────────┘
          │                │                │
          │         ┌──────▼──────┐        │
          │         │  消息队列     │        │
          │         │  (Redis)    │        │
          │         └──────┬──────┘        │
          │                │                │
┌─────────▼─────────────────────────────▼─────────────────────┐
│                      数据存储层                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │  PostgreSQL  │  │    Redis     │  │   阿里云OSS  │     │
│  │  (主数据库)  │  │   (缓存)     │  │  (文件存储)  │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
                           │
                    ┌──────▼──────┐
                    │  AI 能力平台 │
                    ├─────────────┤
                    │ • OCR识别   │
                    │ • NLP服务   │
                    │ • 报告生成  │
                    │ • 知识图谱  │
                    └─────────────┘
```

### 1.2 技术栈

| 层级 | 技术选型 | 说明 |
|-----|---------|------|
| **前端** | React + TypeScript | Web应用 |
| **前端** | React Native | iOS/Android |
| **后端** | Node.js + NestJS | API服务 |
| **数据库** | PostgreSQL | 主数据库 |
| **缓存** | Redis | 会话/缓存 |
| **文件存储** | 阿里云OSS | 图片/文档 |
| **AI OCR** | GLM-OCR | 文字识别 |
| **AI LLM** | DeepSeek/ChatGLM | 智能问答 |
| **部署** | Docker + K8s | 容器化 |
| **CI/CD** | GitHub Actions | 自动化 |

---

## 二、数据库设计

### 2.1 核心表结构

#### 用户表 (users)

```sql
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  username VARCHAR(50) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  real_name VARCHAR(100),
  phone VARCHAR(20),
  email VARCHAR(100),
  department_id UUID REFERENCES departments(id),
  role VARCHAR(20) DEFAULT 'engineer', -- admin, manager, engineer
  avatar_url VARCHAR(500),
  status VARCHAR(20) DEFAULT 'active',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 部门表 (departments)

```sql
CREATE TABLE departments (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(100) NOT NULL,
  code VARCHAR(50) UNIQUE NOT NULL,
  parent_id UUID REFERENCES departments(id),
  description TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 设备表 (equipment)

```sql
CREATE TABLE equipment (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  code VARCHAR(50) UNIQUE NOT NULL,  -- 设备编号
  name VARCHAR(200) NOT NULL,        -- 设备名称
  model VARCHAR(200),                 -- 型号
  manufacturer VARCHAR(200),          -- 制造商
  category_id UUID REFERENCES equipment_categories(id),
  department_id UUID REFERENCES departments(id),
  location VARCHAR(200),              -- 安装位置
  parameters JSONB,                   -- 技术参数
  images JSONB,                       -- 图片
  status VARCHAR(20) DEFAULT 'normal', -- normal, warning, error
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 设备分类表 (equipment_categories)

```sql
CREATE TABLE equipment_categories (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(100) NOT NULL,
  code VARCHAR(50) UNIQUE NOT NULL,
  parent_id UUID REFERENCES equipment_categories(id),
  icon VARCHAR(50),
  description TEXT
);
```

#### 任务模板表 (task_templates)

```sql
CREATE TABLE task_templates (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(200) NOT NULL,
  code VARCHAR(50) UNIQUE NOT NULL,
  category VARCHAR(50),  -- inspection, maintenance, repair
  description TEXT,
  content JSONB NOT NULL,  -- 任务内容定义
  estimated_duration INTEGER,  -- 预计时长(分钟)
  frequency VARCHAR(20),  -- daily, weekly, monthly
  assign_rule JSONB,     -- 分配规则
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 任务表 (tasks)

```sql
CREATE TABLE tasks (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  template_id UUID REFERENCES task_templates(id),
  title VARCHAR(200) NOT NULL,
  description TEXT,
  assigned_to UUID REFERENCES users(id),
  executor_id UUID REFERENCES users(id),
  status VARCHAR(20) DEFAULT 'pending', -- pending, in_progress, completed, cancelled
  priority VARCHAR(20) DEFAULT 'normal', -- low, normal, high, urgent
  scheduled_date DATE NOT NULL,
  scheduled_time TIME,
  completed_at TIMESTAMP,
  completion_data JSONB,  -- 完成任务时填写的数据
  signature_url VARCHAR(500),  -- 签字确认
  photos JSONB,  -- 任务照片
  ai_analysis JSONB,  -- AI分析结果
  notes TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 巡检项表 (inspection_items)

```sql
CREATE TABLE inspection_items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  task_id UUID REFERENCES tasks(id),
  name VARCHAR(200) NOT NULL,
  item_type VARCHAR(50),  -- check, input, capture, photo
  value TEXT,
  unit VARCHAR(20),
  standard_min DECIMAL,
  standard_max DECIMAL,
  is_abnormal BOOLEAN DEFAULT FALSE,
  image_url VARCHAR(500),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 知识库表 (knowledge_base)

```sql
CREATE TABLE knowledge_base (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  title VARCHAR(200) NOT NULL,
  category VARCHAR(50),  -- manual, procedure, case, video
  content TEXT,
  attachments JSONB,  -- 附件列表
  tags JSONB,
  view_count INTEGER DEFAULT 0,
  created_by UUID REFERENCES users(id),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 告警表 (alerts)

```sql
CREATE TABLE alerts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  equipment_id UUID REFERENCES equipment(id),
  alert_type VARCHAR(50),  -- warning, error, info
  title VARCHAR(200) NOT NULL,
  content TEXT,
  level VARCHAR(20),  -- low, medium, high, critical
  status VARCHAR(20) DEFAULT 'active', -- active, acknowledged, resolved
  acknowledged_by UUID REFERENCES users(id),
  acknowledged_at TIMESTAMP,
  resolved_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 学习记录表 (learning_records)

```sql
CREATE TABLE learning_records (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id),
  content_id UUID,
  content_type VARCHAR(50),  -- article, video, course
  progress INTEGER DEFAULT 0,  -- 0-100
  status VARCHAR(20) DEFAULT 'in_progress',
  completed_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 三、API设计

### 3.1 API规范

- 基础路径：`/api/v1`
- 认证方式：Bearer Token
- 数据格式：JSON
- 错误码：见下文

### 3.2 核心API

#### 认证

| 方法 | 路径 | 说明 |
|-----|------|------|
| POST | /auth/login | 登录 |
| POST | /auth/logout | 登出 |
| POST | /auth/refresh | 刷新Token |
| GET | /auth/me | 当前用户 |

#### 用户

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | /users | 用户列表 |
| GET | /users/:id | 用户详情 |
| POST | /users | 创建用户 |
| PUT | /users/:id | 更新用户 |

#### 设备

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | /equipment | 设备列表 |
| GET | /equipment/:id | 设备详情 |
| POST | /equipment | 创建设备 |
| PUT | /equipment/:id | 更新设备 |
| GET | /equipment/:id/history | 设备历史 |

#### 任务

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | /tasks | 任务列表 |
| GET | /tasks/today | 今日任务 |
| GET | /tasks/:id | 任务详情 |
| POST | /tasks | 创建任务 |
| PUT | /tasks/:id | 更新任务 |
| POST | /tasks/:id/complete | 完成任务 |
| POST | /tasks/:id/photos | 上传照片 |

#### OCR

| 方法 | 路径 | 说明 |
|-----|------|------|
| POST | /ocr/recognize | 文字识别 |
| POST | /ocr/analyze | AI分析 |

#### 知识库

| 方法 | 路径 | 说明 |
|-----|------|------|
| GET | /knowledge | 知识列表 |
| GET | /knowledge/:id | 知识详情 |
| POST | /knowledge/search | 搜索 |

#### AI

| 方法 | 路径 | 说明 |
|-----|------|------|
| POST | /ai/chat | 智能问答 |
| POST | /ai/report | 报告生成 |
| POST | /ai/diagnose | 故障诊断 |

---

## 四、AI能力设计

### 4.1 OCR识别服务

```typescript
interface OCRService {
  // 设备铭牌识别
  recognizeNameplate(image: Buffer): Promise<NameplateResult>;
  
  // 仪表读数识别
  recognizeGauge(image: Buffer): Promise<GaugeResult>;
  
  // 阀门挂牌识别
  recognizeValve(image: Buffer): Promise<ValveResult>;
  
  // 管道标识识别
  recognizePipeline(image: Buffer): Promise<PipelineResult>;
}

interface NameplateResult {
  equipmentCode: string;
  equipmentName: string;
  model: string;
  manufacturer: string;
  power: string;
  voltage: string;
  ratedCurrent: string;
  manufactureDate: string;
  confidence: number;
}
```

### 4.2 智能问答服务

```typescript
interface AIService {
  // 知识问答
  chat(question: string, context: ChatContext): Promise<ChatResponse>;
  
  // 故障诊断
  diagnose(symptoms: string[], equipment: Equipment): Promise<DiagnosisResult>;
  
  // 报告生成
  generateReport(type: string, data: ReportData): Promise<string>;
}

interface ChatContext {
  userId: string;
  equipmentId?: string;
  taskId?: string;
  history?: Message[];
}
```

### 4.3 报告生成

```typescript
interface ReportService {
  // 巡检报告
  generateInspectionReport(task: Task): Promise<Report>;
  
  // 故障报告
  generateFaultReport(task: Task, fault: Fault): Promise<Report>;
  
  // 周报/月报
  generatePeriodicReport(user: User, period: Period): Promise<Report>;
}
```

---

## 五、安全设计

### 5.1 认证授权

- JWT Token认证
- Token有效期：24小时
- Refresh Token有效期：7天
- 密码加密：bcrypt

### 5.2 权限控制

| 角色 | 权限 |
|-----|------|
| 管理员 | 全部权限 |
| 经理 | 查看、审核 |
| 工程师 | 执行、记录 |

### 5.3 数据安全

- HTTPS传输
- 敏感数据加密存储
- 操作日志审计

---

## 六、部署架构

### 6.1 开发/测试环境

```
┌─────────────┐
│   开发机    │
└──────┬──────┘
       │
┌──────▼──────┐
│  Docker    │
│  Compose   │
└─────────────┘
```

### 6.2 生产环境

```
                          ┌─────────────┐
                          │   CDN       │
                          └──────┬──────┘
                                 │
                    ┌────────────▼────────────┐
                    │      负载均衡 (Nginx)   │
                    └────────────┬────────────┘
                                 │
        ┌───────────────────────┼───────────────────────┐
        │                       │                       │
┌──────▼──────┐        ┌──────▼──────┐        ┌──────▼──────┐
│   API 1     │        │   API 2     │        │   API 3     │
│  (Docker)   │        │  (Docker)   │        │  (Docker)   │
└──────┬──────┘        └──────┬──────┘        └──────┬──────┘
       │                       │                       │
       └───────────────────────┼───────────────────────┘
                              │
              ┌───────────────┼───────────────┐
              │               │               │
       ┌──────▼──────┐ ┌──────▼──────┐ ┌──────▼──────┐
       │ PostgreSQL  │ │    Redis    │ │     OSS     │
       │   (主从)    │ │   (集群)    │ │   (阿里云)  │
       └─────────────┘ └─────────────┘ └─────────────┘
```

---

## 七、监控运维

### 7.1 监控指标

| 指标 | 说明 |
|-----|------|
| API响应时间 | 平均/P99 |
| 错误率 | 5xx/4xx比例 |
| CPU/内存 | 服务器资源 |
| 数据库连接 | 连接池使用 |

### 7.2 日志

- 访问日志
- 错误日志
- 操作日志
- AI调用日志

---

*文档版本：v1.0*
*最后更新：2026-02-19*
